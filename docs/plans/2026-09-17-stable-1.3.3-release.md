# Stable 1.3.3 (28) publication

On 2026-09-17 the user explicitly requested stable publication of the speaker
accuracy candidate. This supersedes the preceding Beta batch's no-publication
scope; it is not a claim of new human speaker-identity acceptance.

Promote the reviewed Beta implementation to the stable identity without further
product changes. Keep the old Batch 9–16 worktree untouched. Publish source,
the new GitHub release and only the stable appcast. Do not install, launch or
replace the user's local app; no foreground UI automation is authorized.

Distribution remains the existing free local-certificate mode, not Developer
ID or notarized distribution. Preserve the code-signing certificate, Sparkle
public key, existing releases and Beta feed. Sign and independently verify the
exact DMG bytes before exposing the stable feed entry.

## Acceptance boundary

The preceding implementation batch passed background tests and local read-only
audio probes. Two recordings constrained to 4–5 speakers both returned 5
clusters, whereas the user reports one four-person and one five-person meeting.
Counts alone do not establish speaker identity or turn accuracy. Release notes
must retain this limitation and describe the exact-count calibration option.
Historical transcripts without word timing are not automatically retranscribed.
No voiceprint identity library or cloud audio upload is included.

## Release verification

- Fresh background suite on the promoted source: exit 0, 1,425 passed,
  6 opt-in local probes skipped, 0 failed. Debug build-for-testing succeeded.
  UI automation and the 2 interactive panel tests remain NOT RUN.
- Release build/package: exit 0, PACKAGE_VALIDATION=PASS. Mounted identity is
  会议记录 / com.shenminghao.MeetingNotes / 1.3.3 (28), stable feed enabled.
- DMG integrity, mounted deep/strict signatures, embedded Sparkle, sandbox,
  audio input, network client and Sparkle Mach-service permissions PASS.
  No get-task-allow or LSBackgroundOnly test-host flag is present.
- Designated requirement retains certificate SHA1
  1487C197139F45D699244AC63B8F46753CBFF9F3. Embedded Sparkle public key and
  configured signing account are unchanged. APPLE_DISTRIBUTABLE=NO means
  no Developer ID/notarization, not a failed local-certificate package check.
- Sparkle signature verification and independent CryptoKit Ed25519 validation
  PASS; a one-byte-altered archive is rejected. Generated feed adds build 28,
  preserves all 4 old items and removes no update. Beta feed is untouched.
- Artifact: MeetingNotes.dmg, 9,854,077 bytes; SHA256
  207a521618a7f1919b75ff472a2ad332e97e2f68bce03d017cdcad098d245275.
  Previous 1.3.2 artifact is preserved outside the worktree before packaging.
- Beta build 28 artifact and pinned Package.resolved hash remain unchanged.
  No new capture, diagnostics, model catalog, credentials or entitlement edits.
- Reviewed all implementation/configuration/test changes; git diff --check PASS.
  Existing non-blocking screenshot Sendable / AppIntents build warnings are not
  silently treated as fixes or reasons to edit unrelated code.

Public GitHub release, tag/source and live stable-feed checks follow this
source/artifact commit. Local app installation remains explicitly out of scope.
