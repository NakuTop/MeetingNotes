# Stable 1.3.1 (26) release

The user confirmed the accepted Beta functionality is now working, including
live native undo, and explicitly requested stable publication. This authorizes
GitHub release and stable feed publication, not local installation or launch.
Reuse the existing isolated performance worktree; preserve all prior work.

## Plan

1. Audit the accumulated source and tests against `fa7f420`, including independent
   read-only audio, screenshot and editing reviews. Check the existing public
   stable release and both feed identities before any remote write.
2. Change stable identity only to `会议记录 / com.shenminghao.MeetingNotes /
   1.3.1 (26)` in YAML, hand-maintained project and packager; keep Beta 25,
   models, accepted application behavior, signing certificate and Sparkle key.
   Update identity regression expectations first and observe their failure.
3. Run focused identity tests, all permitted background unit tests and Debug
   build. No UI automation or interactive panel tests; these remain NOT RUN.
4. Build a stable DMG using the existing local certificate, validate the mounted
   app, Sparkle loadability, entitlements, stable feed and public signing key.
   Sign its exact bytes with the configured Sparkle Keychain account; verify the
   Ed25519 signature independently. Do not export or rotate private keys.
5. Review/stage only intended source, configuration, tests and release docs;
   create an intentional release commit. Verify remote main is still the
   reviewed ancestor, push normally, publish a new non-prerelease `v1.3.1`
   without replacing older assets, and update only the stable appcast.
6. Re-fetch public release/feed and verify exact asset hash, size, version,
   signature, tag/source commit and unchanged Beta feed. Do not install or launch.

## Distribution policy

Follow the existing `v1.3.0` free local-certificate stable distribution, not the
older ad-hoc `community-unsigned` CI mode. Keep the Developer ID and ad-hoc
validators unchanged: neither describes the existing local-certificate mode.
`PUBLISHABLE=NO` / `APPLE_DISTRIBUTABLE=NO` from the packager mean no Apple
Developer ID/notarization; they must not be relabeled as Apple approval.
The user explicitly accepts this mode and initial manual Gatekeeper allowance.
Free stable publication still requires a valid unchanged code-signing identity,
Sparkle signature, exact artifact metadata and user acceptance.

## Evidence

- Independent scoped source reviews: audio/permission/diagnostics PASS;
  screenshots/annotations PASS; editing/performance/undo PASS; final release
  metadata PASS. No new Critical/Important release blocker identified.
- Identity red: two tests correctly failed before stable promotion (six
  assertions). Identity/release-policy/background-host green: 27 passed,
  0 failed, exit 0.
- Full permitted background units: 1,388 passed, 0 failed, exit 0.
- Debug build: exit 0. UI suite and the two interactive panel tests: NOT RUN.
- Stable packaging: exit 0 / PACKAGE_VALIDATION=PASS; readonly DMG verify and
  mounted deep/strict codesign with the unchanged stable certificate PASS.
- Mounted stable identity: 1.3.1 (26), correct privacy strings, sandbox,
  audio-input/network-client and Sparkle Mach service entitlements. No
  get-task-allow or background-test-host flag. Sparkle embedded and loadable.
- Configured Sparkle Keychain public key matches the unchanged embedded key.
  Generated appcast retains versions 18/16. Both Sparkle verification and
  independent CryptoKit Ed25519 verification PASS; altered bytes rejected.
- Artifact: `MeetingNotes.dmg`, 9,744,696 bytes; SHA256
  `8fb79443848b167c0fb783d8b1ca7eb8b64fa557536cf0ca40dfee0eb2111dbf`.
- Beta 25 artifact and resolved-package hash unchanged. No model, Notion,
  DeepSeek or entitlement changes in this release-promotion step.
- Existing icon-size/Sparkle stripping warnings remain non-fatal; no warning
  cleanup or unrelated feature change was included.

Human Beta acceptance: PASS by the user's explicit report, not by automated
tests. No new product fix is inferred from the live-undo investigation. Remote
publication and post-publication checks follow this source/artifact audit.

## Separate pre-existing follow-up

The editing reviewer noted that a deliberately stale note-edit target equal to
its original text may not restore a previously saved note edit. This predates
this diff; no packaged-app manifestation was established. It was not classified
as a new release blocker and was not changed speculatively during publication.
