# Stable 1.3.4 (32) release authorization and verification

The user explicitly requested publication to the formal in-app update channel
and cleanup of local test products and old installers. Publish the unchanged,
validated candidate through the existing free local-certificate/Sparkle route.
Do not install or replace the local application. Preserve the primary dirty
Batch worktree, meeting data, model caches, signing credentials and Beta feed.

## Candidate and source

- Source starts at 0dee08f on codex/stable-speaker-controls; origin/main matches.
- No new product code or configuration changes during publication.
- Release identity: 会议记录 / com.shenminghao.MeetingNotes / 1.3.4 (32).
- Beta identity and pinned Package.resolved are unchanged.
- Reviewed feature scope: automatic/inferred speaker assignment, local acoustic
  refinement, protected manual overrides, simplified controls, continuous-turn
  display and long-meeting projection/index performance. Unused advanced review
  UI is not exposed; existing voiceprint data management remains available.
- No capture, ASR/model catalog, screenshots, Notion payload or cloud-audio path
  changes. Only approved source/configuration/tests/docs enter the source commit.

## Validation

Reuse the passing background suite from this unchanged source: 1,515 executed,
1,509 passed, 6 opt-in real-audio probes skipped, zero failures, exit 0. Debug
build-for-testing and Release package build both succeeded. UI automation and
the two interactive panel tests were NOT RUN. Human multi-person accuracy and
in-app installation are not inferred from these results.

Fresh publication checks passed: exact DMG SHA/size, hdiutil integrity, mounted
identity and update settings, original certificate designated requirement,
deep/strict nested signatures, arm64, sandbox/audio/network/Sparkle entitlements,
microphone privacy string, embedded Sparkle runtime and absence of test-host
flags. Local signature is not Developer ID signing or notarization.

- Archive: MeetingNotes.dmg, 10,074,695 bytes.
- SHA256: 007e2180342fbeeb755cb0f044c573651379cc23ac321f7f81349647d655c6b1.
- Sparkle signing uses the existing NakuTop.MeetingNotes Keychain account.
  No private key export or rotation.
- Independent CryptoKit Ed25519 verification passed; altered archive rejected.
- Appcast generation adds one item, changes zero old items, removes zero items.
  All prior enclosure signatures/URLs/metadata are preserved.
- Release URL: https://github.com/NakuTop/MeetingNotes/releases/tag/v1.3.4
- Stable feed: https://nakutop.github.io/MeetingNotes/updates/stable/appcast.xml
- Publishing source/tag/release precedes updating the stable feed. Public download
  bytes, signature, deployed feed and untouched Beta feed must be checked again.

## Cleanup boundary

Only inventoried, clearly generated MeetingNotes test products/logs and old or
duplicate DMGs may be moved to Trash after public validation. Preserve source
worktrees, all uncommitted changes, installed production app, meeting recordings,
transcripts, models, signing-key backups and current release evidence. Do not
empty Trash or silently terminate any running application.
